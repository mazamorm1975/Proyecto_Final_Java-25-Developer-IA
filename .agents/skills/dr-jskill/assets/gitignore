# Maven / Java
/target/
!.mvn/wrapper/maven-wrapper.jar
.mvn/timing.properties

# IDEs
*.iml
.idea/
.vscode/
*.classpath
*.project
*.settings/

# Logs
logs/
*.log

# OS
.DS_Store
Thumbs.db

# Node/Vite frontend
frontend/node_modules/
frontend/dist/
frontend/.vite/
frontend/.eslintcache

# Front-end build output. Vite / the Angular CLI write into Spring Boot's static
# folder (`outDir: ../src/main/resources/static`) during `generate-resources`,
# so everything the bundler emits there is generated, never hand-edited.
# The placeholder `index.html` shipped for frontend-less projects stays tracked;
# once you add a frontend/ project the build overwrites it, so add
# `/src/main/resources/static/index.html` here too if you do not want that diff.
/src/main/resources/static/assets/
/src/main/resources/static/*.js
/src/main/resources/static/*.css
/src/main/resources/static/*.map

# Testcontainers / Docker
.testcontainers.properties
**/.testcontainers/*
.docker/
*.pid

# Coverage / reports
coverage/
jacoco.exec
surefire-reports/
failsafe-reports/

# SBOM / scans (regenerate in CI)
cyclonedx-*.xml
cyclonedx-*.json
dependency-check-report.*

# Env files
.env
.env.*
.env.local
!.env.sample

# Native builds
**/*.exe
*.dll
*.so
/target/*-exec

# Misc
*.orig
*.rej

# JDTLS per-project workspace (see .github/lsp.json)
.jdtls-workspace/
